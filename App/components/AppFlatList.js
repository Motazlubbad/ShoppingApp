import React from 'react';
import {FlatList, StyleSheet} from 'react-native';

const AppFlatList = ({...rest}) => {
  return <FlatList {...rest} />;
};

export default AppFlatList;

const styles = StyleSheet.create({});
